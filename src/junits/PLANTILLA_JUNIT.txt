/* Noms a canviar:
	NOM_CAPA		Capa a la que pertany la classe que es prova
	NOM_CLASSE		Nom de la classe que es prova
	NOM_OBJECTE		Nom que tindrà l'objecte de la classe NOM_CLASSE
	
	//La plantilla de la funció s'haurà de compiar per cada una de les funcions de la classe
	
	NOM_FUNCIO		Nom de la funció que es prova. 
	TIPUS_DADES		Tipus de dades que retorna la funció que es prova
	NOM_VARIABLE_SOLUCIO	Nom de la variable que conté el return de la solució
	NOM_EXCEPCIO	Nom de la excepció esperada
*/
package src.junit;

import src.recomanador.NOM_CAPA.NOM_CLASSE;
//tots els imports que es necessiti per comunicar-se amb la classe

import static org.junit.Assert.*;
import org.junit.Test;

public class TestNOM_CLASSE {
	
	/*------Funcions Before / After------*/
	
	@BeforeClass
	public static void beforeClass() {
		//Funcions que s'executaran abans de provar la classe
	}
	
	@AfterClass
	public static void afterClass() {
		//Funcions que s'executaran després de provar la classe
	}
	
	@Before
	public void before() {
		//Funcions que s'executaran abans de cada una de les funcions testejades
	}
	
	@After
	public void after() {
		//Funcions que s'executaran després de cada una de les funcions testejades
	}
	
	/*------Funcions Testos------*/
	
	@Test
	//Si es vol un temps limit:
	//@Test(timeout = <tiempo_en_milisegundos>)
	//Si se sap que es retornarà una excepció i es el que es vol:
	//@Test(expected = NOM_EXCEPCIO.class)
	public void NOM_FUNCIOTest() {
		NOM_CLASSE NOM_OBJECTE = new NOM_CLASSE();
		
		TIPUS_DADES NOM_VARIABLE_SOLUCIO = NOM_OBJECTE.NOM_FUNCIO();
		assertEquals(...);
	}
	
}
